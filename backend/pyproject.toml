[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
filterwarnings = [
    # Vem do Starlette, já corrigido no código deles; remover quando atualizar
    "ignore:The anyio.abc.BlockingPortal alias is deprecated:DeprecationWarning",
]