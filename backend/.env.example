# Copie para .env e ajuste. O arquivo .env NUNCA vai para o Git.

APP_NAME=ERP
ENVIRONMENT=local

# Dentro do Docker, o host do banco é "db" (nome do serviço no docker-compose)
DATABASE_URL=postgresql+psycopg://erp:erp@db:5432/erp

# Lista em formato JSON
CORS_ORIGINS=["http://localhost:3000"]
